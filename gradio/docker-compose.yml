# LawFirmAI Streamlit Docker Compose
# Local development environment for Streamlit interface

version: '3.8'

services:
  lawfirm-streamlit:
    build: .
    ports:
      - "8501:8501"  # Streamlit interface
    environment:
      - PYTHONPATH=/app
      - DATABASE_URL=sqlite:///./data/lawfirm.db
      - CHROMA_DB_PATH=./data/chroma_db
      - LOG_LEVEL=DEBUG
      - DEBUG=true
    volumes:
      - ../data:/app/data
      - ../models:/app/models
      - ../logs:/app/logs
      - ../model_cache:/app/model_cache
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
