# LawFirmAI - HuggingFace Spaces 최적화된 의존성
# Phase 2의 모든 개선사항을 반영한 최적화된 버전

# Core Gradio
gradio==4.0.0

# AI/ML Libraries (최적화된 버전)
torch==2.1.0
transformers==4.35.0
sentence-transformers==2.2.2
faiss-cpu==1.7.4

# LangChain & RAG (필수만 포함)
langchain>=0.1.0
langchain-community>=0.0.10

# Data Processing (최소한만)
pandas==2.1.3
numpy==1.24.3

# Database (SQLite는 내장)
# sqlite3는 Python 내장 모듈이므로 별도 설치 불필요

# API & Web (최소한만)
requests==2.31.0
httpx==0.25.0

# Utilities (필수만)
python-dotenv==1.0.0
pydantic==2.5.0
tqdm==4.66.1

# Logging & Monitoring
structlog==23.2.0

# System Monitoring
psutil==5.9.0

# HuggingFace Spaces 최적화
huggingface-hub>=0.19.0

# 메모리 최적화
gc

# JSON 처리 (내장)
# json은 Python 내장 모듈

# 시간 처리 (내장)
# datetime은 Python 내장 모듈

# 경로 처리 (내장)
# pathlib은 Python 내장 모듈

# 타입 힌트 (내장)
# typing은 Python 내장 모듈

# 컨텍스트 매니저 (내장)
# contextlib은 Python 내장 모듈
