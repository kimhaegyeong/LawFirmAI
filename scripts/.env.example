# Cloudflare R2 설정
# 
# Account ID 찾는 방법:
# 1. Cloudflare 대시보드 (https://dash.cloudflare.com)에 로그인
# 2. 왼쪽 사이드바에서 "R2" 선택
# 3. R2 개요 페이지의 오른쪽 상단에 "Account ID" 표시됨
# 4. 또는 대시보드 오른쪽 사이드바에서도 확인 가능
#
# Access Key ID와 Secret Access Key 찾는 방법:
# 1. Cloudflare 대시보드 > R2 > Manage R2 API Tokens
# 2. "Create API token" 클릭
# 3. 권한 설정 (Object Read & Write 권한 필요)
# 4. 생성된 Access Key ID와 Secret Access Key를 복사
#
CLOUDFLARE_R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
CLOUDFLARE_R2_ACCESS_KEY_ID=<access-key>
CLOUDFLARE_R2_SECRET_ACCESS_KEY=<secret-key>
CLOUDFLARE_R2_BUCKET_NAME=lawfirm-aihub-data

# 로깅 설정
LOG_LEVEL=INFO
TEST_LOG_DIR=logs/test

# MLflow 설정
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=aihub-embeddings
